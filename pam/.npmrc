# StyleX and Astryx resolve peer deps at the workspace root; hoisting keeps a
# single React copy so hooks don't break across packages.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
